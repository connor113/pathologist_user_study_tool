# T-0001: WSI Tiler with Alignment ✅ COMPLETE

**Goal:** Convert SVS to DZI tiles + `manifest.json` with lattice params and `alignment_ok`.

**Status:** ✅ Complete

## Deliverables
- `src/tiler/wsi_tiler.py` - CLI tool for WSI to DZI conversion

## Usage
```bash
python src/tiler/wsi_tiler.py --input slide.svs --out tiles/slide_id --patch-px 256
```

## Output
- DZI pyramid with 512×512 JPEG tiles (Q≈80)
- `manifest.json` with:
  - `slide_id`, `level0_width`, `level0_height`
  - `mpp0`, `patch_px`, `tile_size`, `overlap=0`, `anchor=(0,0)`
  - `alignment_ok=true`, `dzi_level_count`, `magnification_levels`

## Acceptance Criteria ✅
- [x] Tiles written in DZI structure
- [x] Manifest contains all required fields
- [x] Alignment checker verifies lattice math
- [x] Opens in generic DZI viewer
