# tasks/T-0002.md — Viewer load, fit logic, grid overlay & arrows (≤90 min)
**Goal:** TS app loads tiles using manifest; decides start level (5× or lowest that fits via flag); renders world‑fixed grid; enables **arrow panning** by 0.5× viewport; disables free pan & wheel zoom.
**AC:** Grid draws crisp; edge cells disabled; arrow buttons move by half‑screen and clamp; dev overlay shows `(i,j)`.
**Constraints:** OpenSeadragon; no backend; minimal deps.
**Test notes:** Manual checks + unit tests for fit/arrow math.