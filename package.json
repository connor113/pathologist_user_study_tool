{
  "name": "pathologist-viewer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:all": "vitest run && cd backend && npx vitest run"
  },
  "dependencies": {
    "openseadragon": "^4.1.0"
  },
  "devDependencies": {
    "@types/openseadragon": "^3.0.10",
    "typescript": "^5.3.3",
    "vite": "^7.1.9",
    "vitest": "^4.0.18"
  }
}
